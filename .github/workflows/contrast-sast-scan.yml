name: Contrast SAST Scan

on:
  pull_request:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  sast-scan:
    uses: ibm-skills-network/.github/.github/workflows/contrast-sast-scan.yml@main
    secrets: inherit
